{
  "sourceRoot": "apps/plantopi",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/plantopi",
        "outputPath": "dist/apps/plantopi"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "plantopi:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "plantopi:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "plantopi:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "plantopi:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/plantopi"],
      "options": {
        "jestConfig": "apps/plantopi/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/plantopi/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
